import { LinkseguroPipe } from './linkseguro.pipe';

describe('LinkseguroPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkseguroPipe();
    expect(pipe).toBeTruthy();
  });
});
